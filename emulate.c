#include <stdio.h>
#include <stdint.h>
#include "constants.h"

int emulate(uint16_t* memory) {
  uint16_t registers[1 << 2];

  printf("Starting emulation\n");
  printf("%d", MEMSIZE);
  // for(;;) {
  //
  // }
  return 0;
}
